class ChoiceListItemData {
  final String title;
  bool isSelected = false;
  final Object? value;
  ChoiceListItemData({
    required this.title,
    required this.isSelected,
    required this.value,
  });
}
